<!DOCTYPE html>

<title>Client</title>

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Имя</th>
                <td>{{ client.name }}</td>
            </tr>
            <tr>
                <th>Номер телефона</th>
                <td>{{ client.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}">К списку клиентов</a>

    <a href="{{ path('app_client_edit', {'id': client.id}) }}">изменить</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
